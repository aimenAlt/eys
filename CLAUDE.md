@AGENTS.md

## Claude Code

Run from this repo root (`eys/`), not the EYS-Biz hub.

```bash
npm run dev      # local site
npm run check    # astro check
npm run build    # production build
```

`main` deploys via `.github/workflows/deploy-cloudflare.yml`. Do not also connect Cloudflare “Connect to Git”.
